<%= form_for(Favorite.find_by(micropost_id: micropost.id, user_id: current_user.id),
#current_user.favorites.find_by(micropost_id: micropost.id
#
             html: { method: :delete },
             remote: true) do |f| %>

    <%= f.submit "Unfavorite", class: "btn unlike" %>
<% end %>