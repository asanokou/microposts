<%= form_for(Favorite.new(user_id: :current_user.id), remote: true) do |f| %>

    <div>
        <%= hidden_field_tag :micropost_id, @micropost.id>
    </div>
    
    <%= f.submit "Favorite", class: "btn like" %>
<% end %>